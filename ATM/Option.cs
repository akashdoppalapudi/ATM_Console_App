namespace ATM.CLI
{
    public enum Option
    {
        CreateNewAccount = 1,
        ExistingUser,
        Exit,
        Deposit,
        Withdraw,
        Transfer,
        TransactionHistory
    }
}
